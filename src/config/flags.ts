export const _SIQ_Flags = {
  // sanityCheck_EntryOptions
  _SIQ_sanityCheck_EntryOptions: {
    isEnabled: true,        // Enable the sanity check
    expires: {
      isEnabled: true,      // Enable the check for the expires option
      checkPositiv: true,   // Check if the number is positive
      checkExpired: true,   // Check if the entry is expired
    }
  },
}